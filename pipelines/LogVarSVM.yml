name: Log Variance SVM

paradigms:
  - LeftRightImagerySinglePass

pipeline:
  - name: LogVariance
    from: moabb.pipelines.features

  - name: SVC
    from: sklearn.svm
    parameters:
      kernel: 'linear'
