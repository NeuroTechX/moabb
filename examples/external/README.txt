External examples
-----------------

You need to install external dependencies to run these examples. These consist mostly of
various classifier implementations. When using pip, you can use

``pip install moabb[external]``
